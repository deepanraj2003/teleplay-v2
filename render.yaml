services:
  - type: web
    name: teleplay-v2
    env: python
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "chmod +x start.sh && ./start.sh"
    envVars:
      - key: PORT
        value: 10000
